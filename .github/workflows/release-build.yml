name: Release Build

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: release-build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x64
            os: windows-2022
            qt_host: windows
            qt_arch: win64_msvc2022_64
            package_kind: windows
          - name: macOS arm64
            os: macos-14
            qt_host: mac
            qt_arch: clang_64
            package_kind: macos
          - name: Linux x86_64
            os: ubuntu-22.04
            qt_host: linux
            qt_arch: linux_gcc_64
            package_kind: linux

    env:
      BUILD_CONFIG: Release
      FFVER: "7.1.1"
      FFMPEG_MIN_ROOT: ${{ github.workspace }}/third_party/ffmpeg-min
      PACKAGE_ID: ${{ github.ref_type == 'tag' && github.ref_name || github.sha }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux build dependencies
        if: matrix.package_kind == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build pkg-config yasm nasm zlib1g-dev libbz2-dev

      - name: Install macOS build dependencies
        if: matrix.package_kind == 'macos'
        run: brew install ninja nasm pkg-config

      - name: Setup MSVC
        if: matrix.package_kind == 'windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Setup MSYS2
        if: matrix.package_kind == 'windows'
        id: msys2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            make
            diffutils
            pkgconf
            tar
            git
            nasm
          path-type: inherit

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.8.0"
          host: ${{ matrix.qt_host }}
          target: desktop
          arch: ${{ matrix.qt_arch }}
          cache: "true"

      - name: Build minimal static FFmpeg (Unix)
        if: matrix.package_kind != 'windows'
        shell: bash
        run: ./scripts/build-ffmpeg-min.sh

      - name: Build minimal static FFmpeg (Windows)
        if: matrix.package_kind == 'windows'
        shell: pwsh
        env:
          MSYS2_ROOT: ${{ steps.msys2.outputs.msys2-location }}
        run: powershell -ExecutionPolicy Bypass -File scripts/build-ffmpeg-min-windows.ps1 -Msys2Root "$env:MSYS2_ROOT"

      - name: Configure and build (Windows)
        if: matrix.package_kind == 'windows'
        shell: pwsh
        run: |
          cmake -S . -B build -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_PREFIX_PATH="$env:QT_ROOT_DIR" `
            -DFFMPEG_MIN_ROOT="$env:FFMPEG_MIN_ROOT" `
            -DCMAKE_CONFIGURATION_TYPES="$env:BUILD_CONFIG" `
            -DBUILD_TESTING=OFF
          cmake --build build --config "$env:BUILD_CONFIG" --parallel

      - name: Package Windows x64
        if: matrix.package_kind == 'windows'
        shell: pwsh
        run: |
          $guiPackageDir = "artifact/CCTVVideoDownloader"
          $guiZipPath = "artifact/CCTVVideoDownloader.$env:PACKAGE_ID.win.x64.zip"
          $cliPackageDir = "artifact/cctv-dl"
          $cliZipPath = "artifact/cctv-dl.$env:PACKAGE_ID.win.x64.zip"
          New-Item -ItemType Directory -Path $guiPackageDir, "$cliPackageDir/bin" -Force | Out-Null
          Copy-Item "build/$env:BUILD_CONFIG/CCTVVideoDownloader.exe" $guiPackageDir
          Copy-Item "build/$env:BUILD_CONFIG/cctv-dl.exe" "$cliPackageDir/bin"
          & "$env:QT_ROOT_DIR/bin/windeployqt.exe" --release --no-translations "$guiPackageDir/CCTVVideoDownloader.exe"
          & "$env:QT_ROOT_DIR/bin/windeployqt.exe" --release --no-translations "$cliPackageDir/bin/cctv-dl.exe"
          $vcRuntimeDir = Join-Path $env:VCToolsRedistDir "x64\Microsoft.VC143.CRT"
          foreach ($runtime in "MSVCP140.dll", "VCRUNTIME140.dll", "VCRUNTIME140_1.dll") {
            $source = Join-Path $vcRuntimeDir $runtime
            if (-not (Test-Path $source)) { throw "MSVC runtime missing from toolchain: $source" }
            Copy-Item $source $guiPackageDir
            Copy-Item $source "$cliPackageDir/bin"
          }
          if (-not (Test-Path (Join-Path $guiPackageDir "platforms/qwindows.dll"))) {
            throw "windeployqt did not deploy the Windows GUI platform plugin"
          }
          if (-not (Test-Path (Join-Path $cliPackageDir "bin/cctv-dl.exe"))) {
            throw "CLI executable missing from Windows CLI release package"
          }
          foreach ($dependency in "Qt6Core.dll", "Qt6Network.dll") {
            if (-not (Test-Path (Join-Path $cliPackageDir "bin/$dependency"))) {
              throw "Windows CLI runtime dependency missing: $dependency"
            }
          }
          if (-not (Test-Path (Join-Path $cliPackageDir "bin/tls/qschannelbackend.dll"))) {
            throw "Windows CLI TLS backend missing"
          }
          $savedPath = $env:PATH
          $env:PATH = (Resolve-Path "$cliPackageDir/bin").Path
          try {
            & "$cliPackageDir/bin/cctv-dl.exe" --version
          } finally {
            $env:PATH = $savedPath
          }
          if (Test-Path (Join-Path $guiPackageDir "cctv-dl.exe")) {
            throw "GUI release package must not contain cctv-dl.exe"
          }
          Compress-Archive -Path "$guiPackageDir/*" -DestinationPath $guiZipPath
          Compress-Archive -Path $cliPackageDir -DestinationPath $cliZipPath
          Invoke-WebRequest -Uri "https://aka.ms/vs/17/release/vc_redist.x64.exe" -OutFile "artifact/vc_redist.x64.exe"

      - name: Configure, build, and install (Unix)
        if: matrix.package_kind != 'windows'
        shell: bash
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE="${BUILD_CONFIG}" \
            -DCMAKE_PREFIX_PATH="${QT_ROOT_DIR}" \
            -DFFMPEG_MIN_ROOT="${FFMPEG_MIN_ROOT}" \
            -DBUILD_TESTING=OFF
          cmake --build build --parallel
          cmake --install build --component gui --prefix "${GITHUB_WORKSPACE}/artifact/CCTVVideoDownloader"
          cmake --install build --component cli --prefix "${GITHUB_WORKSPACE}/artifact/cctv-dl"

      - name: Package macOS arm64
        if: matrix.package_kind == 'macos'
        shell: bash
        run: |
          app="artifact/CCTVVideoDownloader/CCTVVideoDownloader.app"
          zip="artifact/CCTVVideoDownloader.${PACKAGE_ID}.macos.arm64.zip"
          dmg="artifact/CCTVVideoDownloader.${PACKAGE_ID}.macos.arm64.dmg"
          test -x "$app/Contents/MacOS/CCTVVideoDownloader"
          test ! -e "$app/Contents/MacOS/cctv-dl"
          test -d "$app/Contents/Frameworks/QtCore.framework"
          test -f "$app/Contents/PlugIns/platforms/libqcocoa.dylib"
          # Seal the complete deployed bundle. The linker signature on the main
          # executable alone is invalid once frameworks and resources are added.
          codesign --force --deep --sign - "$app"
          codesign --verify --deep --strict --verbose=2 "$app"
          python3 - "$app" <<'PY'
          import subprocess, sys, time
          process = subprocess.Popen([f"{sys.argv[1]}/Contents/MacOS/CCTVVideoDownloader"])
          time.sleep(5)
          if process.poll() is not None:
              raise SystemExit(f"macOS app exited with {process.returncode}")
          process.terminate()
          process.wait(timeout=5)
          PY
          # Keep the portable .app zip, and also publish a guided installer DMG.
          ditto -c -k --sequesterRsrc --keepParent "$app" "$zip"
          brew list --versions imagemagick >/dev/null 2>&1 || brew install imagemagick
          chmod +x scripts/package-macos-dmg.sh
          ./scripts/package-macos-dmg.sh \
            "$app" \
            "$dmg" \
            packaging/macos/dmg-background.png
          test -f "$dmg"
          chmod +x scripts/package-macos-cli.sh
          ./scripts/package-macos-cli.sh \
            artifact/cctv-dl \
            artifact/cctv-dl.${PACKAGE_ID}.macos.arm64.tar.gz

      - name: Package Linux x86_64
        if: matrix.package_kind == 'linux'
        shell: bash
        run: |
          package_dir="artifact/CCTVVideoDownloader"
          archive="artifact/CCTVVideoDownloader.${PACKAGE_ID}.linux.x86_64.tar.gz"
          appimage="artifact/CCTVVideoDownloader.${PACKAGE_ID}.linux.x86_64.AppImage"
          test -x "$package_dir/bin/CCTVVideoDownloader"
          test ! -e "$package_dir/bin/cctv-dl"
          find "$package_dir" -name 'libQt6Core.so*' -print -quit | grep -q .
          find "$package_dir" -path '*/platforms/libqxcb.so' -print -quit | grep -q .
          tar -C artifact -czf "$archive" CCTVVideoDownloader
          # Keep the portable tarball and also publish a download-and-run AppImage.
          chmod +x scripts/package-linux-appimage.sh
          ./scripts/package-linux-appimage.sh \
            "$package_dir" \
            "$appimage" \
            packaging/linux/cctvvideodownloader.desktop \
            packaging/linux/cctvvideodownloader.png
          test -x "$appimage"
          cli_dir="artifact/cctv-dl"
          cli_archive="artifact/cctv-dl.${PACKAGE_ID}.linux.x86_64.tar.gz"
          test -x "$cli_dir/bin/cctv-dl"
          find "$cli_dir" -name 'libQt6Core.so*' -print -quit | grep -q .
          env -u LD_LIBRARY_PATH -u QT_PLUGIN_PATH -u QML2_IMPORT_PATH -u QML_IMPORT_PATH \
            "$cli_dir/bin/cctv-dl" --version
          tar -C artifact -czf "$cli_archive" cctv-dl

      - name: Upload useful release packages
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.package_kind }}
          path: |
            artifact/CCTVVideoDownloader.*.win.x64.zip
            artifact/CCTVVideoDownloader.*.macos.arm64.zip
            artifact/CCTVVideoDownloader.*.macos.arm64.dmg
            artifact/CCTVVideoDownloader.*.linux.x86_64.tar.gz
            artifact/CCTVVideoDownloader.*.linux.x86_64.AppImage
            artifact/cctv-dl.*.win.x64.zip
            artifact/cctv-dl.*.macos.arm64.tar.gz
            artifact/cctv-dl.*.linux.x86_64.tar.gz
            artifact/vc_redist.x64.exe
          if-no-files-found: error
